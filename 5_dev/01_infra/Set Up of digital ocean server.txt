1. Create new basic droplet in digital ocean account

2. Create an SHH Key using Putty (Used and old one)

3. Changed the droplet root password

Root password: queenAlgo2021!

4. Access uding SSH (Putty)

key:DigitalOcean_CB, pass: Alfeios051182!

5. Create Swap Space on Ubuntu 20.04 (Increasing virtual RAM)
https://www.digitalocean.com/community/tutorials/how-to-add-swap-space-on-ubuntu-20-04
https://devanswers.co/creating-swap-space-ubuntu-18-04/

swapon --show

sudo fallocate -l 5G /swapfile
ls -lh /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

sudo sysctl vm.swappiness=10
sudo sysctl vm.vfs_cache_pressure=50

sudo nano /etc/sysctl.conf
Add this to the bottom of /etc/sysctl.conf
vm.swappiness=10
vm.vfs_cache_pressure=50


6. Create a new user and set a firewirewall and copy ssh

From: https://www.digitalocean.com/community/tutorials/initial-server-setup-with-ubuntu-18-04

-- sudo adduser kblancov
-- usermod -aG sudo kblancov

--ufw allow OpenSSH

--ufw enable

-- rsync --archive --chown=kblancov:kblancov ~/.ssh /home/kblancov


7. Give FTPS access to folder /var/www

-- sudo adduser kblancov www-data
-- sudo chmod g+rwX -R /var/www
-- sudo ln -s /var/www /home/kblancov/

8. Update Ubuntu server

-- sudo apt-get update
-- sudo apt-get upgrade

9. Acess using FTP (Fillezilla)
